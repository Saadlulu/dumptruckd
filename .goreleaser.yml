project_name: dumptruckd

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - env:
      - CGO_ENABLED=0
    main: ./cmd/dumptruckd
    binary: dumptruckd
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - 6
      - 7
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ with .Arm }}v{{ . }}{{ end }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - install.sh
      - config/dumptruckd.toml.example
      - config/example-single-file.toml
      - examples/dumptruckd.service

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

# Homebrew tap
brews:
  - repository:
      owner: Saadlulu
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/Saadlulu/dumptruckd"
    description: "A modular database backup daemon"
    license: "MIT"
    dependencies:
      - name: postgresql
        type: optional
      - name: mysql-client
        type: optional
    install: |
      bin.install "dumptruckd"
      etc.install "config/dumptruckd.toml.example" => "dumptruckd/dumptruckd.toml.example"
      etc.install "config/example-single-file.toml" => "dumptruckd/example-single-file.toml"
      etc.install "examples/dumptruckd.service" => "dumptruckd/dumptruckd.service"
    post_install: |
      (var/"backups/dumptruckd").mkpath
      (var/"log").mkpath
    test: |
      system "#{bin}/dumptruckd", "-version"
    service: |
      run [opt_bin/"dumptruckd", "-config", etc/"dumptruckd/dumptruckd.toml"]
      keep_alive true
      log_path var/"log/dumptruckd.log"
      error_log_path var/"log/dumptruckd.log"

# Deb and RPM packages
nfpms:
  - package_name: dumptruckd
    vendor: dumptruckd
    homepage: https://github.com/Saadlulu/dumptruckd
    description: A modular database backup daemon
    license: MIT
    maintainer: dumptruckd maintainers
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    recommends:
      - postgresql-client
    contents:
      - src: config/dumptruckd.toml.example
        dst: /etc/dumptruckd/dumptruckd.toml.example
        type: config
      - src: config/example-single-file.toml
        dst: /etc/dumptruckd/example-single-file.toml
        type: config
      - src: config/config.d/databases.toml
        dst: /etc/dumptruckd/config.d/databases.toml
        type: config
      - src: config/config.d/uploaders.toml
        dst: /etc/dumptruckd/config.d/uploaders.toml
        type: config
      - src: config/config.d/compressors.toml
        dst: /etc/dumptruckd/config.d/compressors.toml
        type: config
      - src: config/config.d/retention.toml
        dst: /etc/dumptruckd/config.d/retention.toml
        type: config
      - src: config/config.d/backups.toml
        dst: /etc/dumptruckd/config.d/backups.toml
        type: config
      - src: examples/dumptruckd.service
        dst: /lib/systemd/system/dumptruckd.service
        type: config
    scripts:
      postinstall: packaging/postinstall.sh

# Docker images
dockers:
  - goos: linux
    goarch: amd64
    image_templates:
      - "ghcr.io/saadlulu/dumptruckd:{{ .Tag }}"
      - "ghcr.io/saadlulu/dumptruckd:latest"
    dockerfile: Dockerfile.goreleaser
    extra_files:
      - config/example-single-file.toml
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.Commit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
